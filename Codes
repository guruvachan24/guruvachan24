<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Message for Vandana</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(to right, #1f1c2c, #928dab);
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
      font-family: 'Segoe UI', sans-serif;
    }

    .msg-box {
      text-align: center;
      color: #fff;
      animation: fadeIn 2s ease-in-out forwards;
    }

    h1 {
      font-size: 3rem;
      color: #ffccf9;
      text-shadow: 0 0 15px #f0f;
      animation: glow 2s ease-in-out infinite alternate;
    }

    p {
      font-size: 1.3rem;
      margin-top: 15px;
      color: #ffe6f7;
      animation: slideUp 2s ease-out;
    }

    @keyframes glow {
      from { text-shadow: 0 0 10px #f0f, 0 0 20px #f0f; }
      to { text-shadow: 0 0 25px #ff99cc, 0 0 40px #ff99cc; }
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: scale(1.1); }
      to { opacity: 1; transform: scale(1); }
    }

    @keyframes slideUp {
      from { transform: translateY(50px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }
  </style>
</head>
<body>
  <div class="msg-box">
    <h1>Dear Vandana 🌸</h1>
    <p>You are a beautiful soul, full of grace and light.<br>
    This world is brighter with you in it. 💫<br>
    Keep smiling, keep shining! 💖</p>
  </div>
</body>
</html>
