<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      background-color: #111;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .neon-btn {
      color: #fff;
      background: #111;
      border: 2px solid #0ff;
      padding: 15px 30px;
      font-size: 1.2em;
      text-transform: uppercase;
      border-radius: 10px;
      box-shadow: 0 0 10px #0ff, 0 0 20px #0ff, 0 0 40px #0ff;
      transition: 0.3s;
    }
    .neon-btn:hover {
      box-shadow: 0 0 20px #0ff, 0 0 30px #0ff, 0 0 60px #0ff;
    }
  </style>
</head>
<body>
  <button class="neon-btn">Click Me</button>
</body>
</html>
