<!DOCTYPE html>
<html lang="en">
<head>
  <style>
    body {
      margin: 0;
      background: #000;
      overflow: hidden;
    }

    .heart {
      width: 50px;
      height: 50px;
      background: red;
      position: absolute;
      top: 100vh;
      left: 50%;
      transform: translateX(-50%) rotate(-45deg);
      animation: floatUp 5s linear infinite;
      opacity: 0.8;
    }

    .heart::before,
    .heart::after {
      content: '';
      width: 50px;
      height: 50px;
      background: red;
      position: absolute;
      border-radius: 50%;
    }

    .heart::before {
      top: -25px;
      left: 0;
    }

    .heart::after {
      top: 0;
      left: 25px;
    }

    @keyframes floatUp {
      0% {
        transform: translate(-50%, 0) rotate(-45deg);
        opacity: 1;
      }
      100% {
        transform: translate(-50%, -100vh) rotate(-45deg);
        opacity: 0;
      }
    }
  </style>
</head>
<body>
  <!-- You can duplicate this div for more hearts -->
  <div class="heart" style="left: 30%; animation-delay: 0s;"></div>
  <div class="heart" style="left: 60%; animation-delay: 1s;"></div>
  <div class="heart" style="left: 80%; animation-delay: 2s;"></div>
  <div class="
